{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/phonebook-backend && cd ../../part3/phonebook-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kaaato",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
